# 🍯 دليل استخدام AI للتوصيات - للمبتدئين

## 🎯 شنو هاد AI؟

تخيل عندك **نادل ذكي** في مقهى:
- كل ما تزور المقهى، يتذكر شنو طلبتي
- مع الوقت، يعرف أنك كتحبي القهوة بالحليب
- يبدأ يقترح عليك حاجات جديدة غادي تعجبك

**هاد AI كيدير نفس الشي لمتجرك!**

---

## 🔧 المكونات الأساسية (3 فقط!)

```
┌─────────────────┐     ┌─────────────────┐     ┌─────────────────┐
│   1. المنتجات   │ ──► │  2. الزبائن     │ ──► │  3. التوصيات    │
│   (Products)    │     │   (Users)       │     │ (Recommendations)│
└─────────────────┘     └─────────────────┘     └─────────────────┘
```

### 1️⃣ المنتجات (Products)
منتجاتك مع معلوماتها:
```
عسل السدر:
  - الفئة: عسل طبيعي
  - الكلمات: صحة، مناعة، طبيعي
  - السعر: 450
```

### 2️⃣ الزبائن (Users)
كل زبون عندو ملف:
```
أحمد:
  - اهتماماته: صحة، طبيعي
  - شاف: عسل السدر، عسل الجبال
  - اشترى: عسل السدر
```

### 3️⃣ التوصيات (Recommendations)
AI يقارن ويقترح:
```
لأحمد نقترح:
  - عسل مانوكا (85%) ← لأنه صحي مثل السدر
  - عسل الحبة السوداء (70%) ← لأنه للمناعة
```

---

## 🎮 كيفاش تجربو عملياً؟

### الخطوة 1: شغّل الخادم

افتح Terminal واكتب:
```bash
cd "/home/user/│── content_recommender.py"
python3 real_api_server.py
```

### الخطوة 2: افتح المتجر

في متصفح، افتح:
```
http://localhost:8080/honey_store.html
```

### الخطوة 3: جرّب!

1. **اضغط على منتج** → AI يسجل أنك شفتيه
2. **أضف للسلة** → AI يفهم أنك حبيتيه
3. **شوف التوصيات** (يمين الشاشة) → AI يقترح منتجات مشابهة
4. **بدّل الزبون** → شوف توصيات مختلفة

---

## 📊 كيفاش AI كيفكر؟

### مثال بسيط:

```
الزبون: أحمد (محب الصحة)
اهتماماته: [صحة، مناعة]

المنتجات:
┌──────────────────┬─────────────────┬────────────┐
│ المنتج           │ الكلمات         │ التطابق    │
├──────────────────┼─────────────────┼────────────┤
│ عسل مانوكا       │ صحة، مناعة      │ 90% ✅     │
│ عسل الحبة السوداء │ صحة، مناعة      │ 85% ✅     │
│ عسل زهور         │ حلو، عائلة      │ 30% ❌     │
└──────────────────┴─────────────────┴────────────┘

→ AI يقترح: مانوكا + الحبة السوداء
```

### الصيغة البسيطة:
```
النقطة = (تطابق الكلمات × 40%) + (مشابه للي اشتراهم × 30%) + (رائج × 20%) + (جديد × 10%)
```

---

## 🔌 كيفاش تدمجو في موقعك؟

### A. إذا عندك موقع HTML بسيط:

```html
<script>
// 1. لما الزبون يشوف منتج
function viewProduct(productId) {
    fetch('http://localhost:5050/api/interact', {
        method: 'POST',
        headers: {'Content-Type': 'application/json'},
        body: JSON.stringify({
            user_id: 'ahmed',      // ID الزبون
            content_id: productId, // ID المنتج
            type: 'view'           // نوع التفاعل
        })
    });
}

// 2. لما يضيف للسلة
function addToCart(productId) {
    fetch('http://localhost:5050/api/interact', {
        method: 'POST',
        body: JSON.stringify({
            user_id: 'ahmed',
            content_id: productId,
            type: 'like',
            rating: 5
        })
    });
}

// 3. جلب التوصيات
async function getRecommendations() {
    const response = await fetch('http://localhost:5050/api/recommendations/ahmed');
    const data = await response.json();
    console.log(data.recommendations);
    // [
    //   { id: 'manuka', title: 'عسل مانوكا', score: 0.85 },
    //   { id: 'black_seed', title: 'عسل الحبة السوداء', score: 0.70 }
    // ]
}
</script>
```

### B. إذا عندك Laravel:

```php
// في Controller
use Illuminate\Support\Facades\Http;

class ProductController extends Controller
{
    // تسجيل مشاهدة
    public function view($productId)
    {
        Http::post('http://localhost:5050/api/interact', [
            'user_id' => auth()->id(),
            'content_id' => $productId,
            'type' => 'view'
        ]);
    }

    // جلب التوصيات
    public function recommendations()
    {
        $userId = auth()->id();
        $response = Http::get("http://localhost:5050/api/recommendations/{$userId}");
        return $response->json()['recommendations'];
    }
}
```

### C. إذا عندك React:

```jsx
// hooks/useAI.js
const API = 'http://localhost:5050';

export function useAI(userId) {
    // تسجيل تفاعل
    const trackInteraction = async (productId, type) => {
        await fetch(`${API}/api/interact`, {
            method: 'POST',
            headers: {'Content-Type': 'application/json'},
            body: JSON.stringify({
                user_id: userId,
                content_id: productId,
                type: type
            })
        });
    };

    // جلب التوصيات
    const getRecommendations = async () => {
        const res = await fetch(`${API}/api/recommendations/${userId}`);
        const data = await res.json();
        return data.recommendations;
    };

    return { trackInteraction, getRecommendations };
}

// في Component
function ProductCard({ product }) {
    const { trackInteraction } = useAI('ahmed');
    
    return (
        <div onClick={() => trackInteraction(product.id, 'view')}>
            <h3>{product.name}</h3>
            <button onClick={() => trackInteraction(product.id, 'like')}>
                أضف للسلة
            </button>
        </div>
    );
}
```

---

## 📡 الـ API Endpoints (الروابط)

| الرابط | الوظيفة | مثال |
|--------|---------|------|
| `POST /api/interact` | تسجيل تفاعل | زبون شاف/اشترى منتج |
| `GET /api/recommendations/{user_id}` | جلب توصيات | اقتراحات لزبون معين |
| `POST /api/content` | إضافة منتج | إضافة منتج جديد |
| `GET /api/stats` | إحصائيات | عدد الزبائن والمنتجات |

---

## 🧪 تجربة سريعة بـ curl

افتح Terminal وجرب:

```bash
# 1. شوف الإحصائيات
curl http://localhost:5050/api/stats

# 2. سجّل أن أحمد شاف عسل السدر
curl -X POST http://localhost:5050/api/interact \
  -H "Content-Type: application/json" \
  -d '{"user_id":"ahmed", "content_id":"sidr_royal", "type":"view"}'

# 3. جلب توصيات لأحمد
curl http://localhost:5050/api/recommendations/ahmed
```

---

## 💡 نصائح مهمة

### ✅ افعل:
- سجّل كل تفاعل (view, like, purchase)
- أضف tags/كلمات لكل منتج
- حدد اهتمامات الزبون من البداية

### ❌ لا تفعل:
- لا تنسى تشغيل الخادم
- لا ترسل بيانات فارغة
- لا تتوقع نتائج من أول تفاعل (AI يحتاج بيانات)

---

## 🎯 الخلاصة

```
1. الزبون يتفاعل (يشاهد/يشتري) 📱
           ↓
2. API يستقبل التفاعل 📡
           ↓
3. AI يحلل ويتعلم 🧠
           ↓
4. AI يقترح منتجات 💡
           ↓
5. الزبون يشوف توصيات 🎯
```

**كلما زاد التفاعل ← كلما زادت دقة التوصيات!**

---

## 📞 محتاج مساعدة؟

إذا واجهتك مشكلة، قولي:
- "الخادم ما خدمش" ← نشغلو
- "التوصيات فارغة" ← نضيف بيانات
- "بغيت ندمج مع Laravel" ← نشرح بالتفصيل
- "بغيت نفهم الكود" ← نشرح أي جزء

**أنا هنا نعاونك!** 💪🍯
